[pytest]
pythonpath = ..
testpaths = .
asyncio_mode = auto
markers =
    slow: marks tests as slow (rate limit, OCR heavy)
    unit: pure unit tests (no server required)
    integration: end-to-end tests (server must be running)
